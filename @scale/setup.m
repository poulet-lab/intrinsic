function varargout = setup(obj)
% open GUI to change DAQ settings.

nargoutchk(0,1)

% create controls
sw = settingsWindow(...
    'WindowStyle',  'normal', ...
    'Visible',      'off', ...
    'Name',         'Scale Settings', ...
    'Position',     [100 100 250 10]);
sp = sw.addPanel();
controls.adaptor = sp.addEdit( ...
    'Label',        'Adaptor', ...
    'Enable',       'off');
controls.camera = sp.addEdit( ...
    'Label',        'Camera', ...
    'Enable',       'off');
controls.mode = sp.addPopupmenu( ...
    'Label',        'Mode', ...
    'Callback',     @obj.cbMode, ...
    'String',       obj.Camera.modes);
controls.scale = sp.addPopupmenu( ...
    'Label',        'Magnification', ...
    'Callback',     [], ...
    'String',       {'1','2','3'});
[controls.okay,controls.cancel] = sw.addOKCancel();
movegui(sw.Handle,'center')
sw.Visible = 'on';

% store UI control objects
obj.Figure = sw.Handle;
setappdata(obj.Figure,'controls',controls);

% initialize
%obj.cbVendor(ctrl.vendor)

% output arguments
if nargout == 1
    varargout{1} = obj.Figure;
end